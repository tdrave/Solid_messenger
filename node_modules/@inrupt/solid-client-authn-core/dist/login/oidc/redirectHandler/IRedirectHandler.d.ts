/// <reference types="node" />
import { fetch } from "cross-fetch";
import { JWK } from "jose";
import { EventEmitter } from "events";
import IHandleable from "../../../util/handlerPattern/IHandleable";
import { ISessionInfo } from "../../../sessionInfo/ISessionInfo";
export declare type RedirectResult = ISessionInfo & {
    fetch: typeof fetch;
};
export declare type RedirectInput = [string, EventEmitter | undefined];
declare type IRedirectHandler = IHandleable<RedirectInput, RedirectResult>;
export default IRedirectHandler;
export declare function fetchJwks(jwksIri: string, issuerIri: string): Promise<JWK>;
export declare function getWebidFromTokenPayload(idToken: string, jwksIri: string, issuerIri: string, clientId: string): Promise<string>;
