import { ILoginOptions, ILoginHandler, LoginResult } from "@inrupt/solid-client-authn-core";
export default class WebIdLoginHandler implements ILoginHandler {
    canHandle(_loginOptions: ILoginOptions): Promise<boolean>;
    handle(_loginOptions: ILoginOptions): Promise<LoginResult>;
}
