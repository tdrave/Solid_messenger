/// <reference types="node" />
import { IRedirectHandler, ISessionInfo } from "@inrupt/solid-client-authn-core";
import type { EventEmitter } from "events";
export declare class ErrorOidcHandler implements IRedirectHandler {
    canHandle(redirectUrl: string): Promise<boolean>;
    handle(redirectUrl: string, eventEmitter?: EventEmitter): Promise<ISessionInfo & {
        fetch: typeof fetch;
    }>;
}
