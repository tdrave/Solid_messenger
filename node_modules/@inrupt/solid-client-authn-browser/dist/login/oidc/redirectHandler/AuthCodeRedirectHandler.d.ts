/// <reference types="node" />
import { IClientRegistrar, IIssuerConfigFetcher, IRedirectHandler, ISessionInfo, ISessionInfoManager, IStorageUtility, ITokenRefresher } from "@inrupt/solid-client-authn-core";
import { EventEmitter } from "events";
export declare const DEFAULT_LIFESPAN: number;
export declare class AuthCodeRedirectHandler implements IRedirectHandler {
    private storageUtility;
    private sessionInfoManager;
    private issuerConfigFetcher;
    private clientRegistrar;
    private tokerRefresher;
    constructor(storageUtility: IStorageUtility, sessionInfoManager: ISessionInfoManager, issuerConfigFetcher: IIssuerConfigFetcher, clientRegistrar: IClientRegistrar, tokerRefresher: ITokenRefresher);
    canHandle(redirectUrl: string): Promise<boolean>;
    handle(redirectUrl: string, eventEmitter?: EventEmitter): Promise<ISessionInfo & {
        fetch: typeof fetch;
    }>;
}
